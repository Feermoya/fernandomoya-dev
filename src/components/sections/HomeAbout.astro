---
import AboutSectionClient from '@/components/react/AboutSectionClient';
import { site } from '@/data/site';
import { getImage } from 'astro:assets';
import profilePhoto from '@/assets/profile.png';

const profileSrc = (await getImage({ src: profilePhoto, width: 640, height: 640 })).src;
const aboutBody =
  'Arranco cada proyecto con una llamada o brief corto para entender bien qué necesitás antes de escribir una sola línea de código. Trabajo con negocios de todo tipo.';
---

<AboutSectionClient
  client:visible
  profileSrc={profileSrc}
  profileAlt={`Retrato de ${site.name}`}
  heading="Diseño webs que explican mejor tu negocio"
  body={aboutBody}
  linkedin={site.social.linkedin}
  email={site.email}
  metrics={[...site.metrics]}
/>
